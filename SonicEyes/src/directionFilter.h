#ifndef DIRECTION_FILTER_H
#define DIRECTION_FILTER_H

#include "filterBaseClass.h"

class directionFilter : public filterBaseClass
{
    public:
        directionFilter();
        ~directionFilter();
};

#endif
