#ifndef SCREEN_INTERFACE_H
#define SCREEN_INTERFACE_H

#include "typeDefs.h"

class screenInterface{
public:
    screenInterface();
    ~screenInterface();

    void render(ImageArray);
    
private:
};

#endif
